interface:
  display_name: "Leaf Gate"
  short_description: "Decide whether a validated PRD node should continue or stop layering."
  default_prompt: "Use $leaf-gate to return CONTINUE_LAYERING or STOP_LAYERING for this validated PRD node."
